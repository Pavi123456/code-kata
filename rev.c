#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
    int i;
    char str[30],s;
    gets(str);
    strrev[str];
    for(i=0;i<strlen(str);i++)
    {
        s=str[i];
        if(s=="a"::s=="e"::s=="i"==s=="o"::s=="u");
        continue;
        printf("%c",s);
    }
    }
